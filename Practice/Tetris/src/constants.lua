CELL_SIZE = 8

LEFT_PADDING_COLUMNS = 1
BORDER_COLUMNS = 1
GRID_COLUMNS = 10
GRID_ROWS = 18
INFO_COLUMNS = 9

COLUMNS = LEFT_PADDING_COLUMNS + BORDER_COLUMNS * 2 + GRID_COLUMNS + INFO_COLUMNS
ROWS = GRID_ROWS

VIRTUAL_WIDTH = CELL_SIZE * COLUMNS
VIRTUAL_HEIGHT = CELL_SIZE * ROWS

local SCALE = math.floor(700 / VIRTUAL_WIDTH)

WINDOW_WIDTH = VIRTUAL_WIDTH * SCALE
WINDOW_HEIGHT = VIRTUAL_HEIGHT * SCALE
